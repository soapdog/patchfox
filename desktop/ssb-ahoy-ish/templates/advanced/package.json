{
  "name": "advanced-ahoy",
  "version": "0.1.0",
  "description": "This example app shows you a minimal setup covering: - package.json scripts - electron-builder config     - a good starter config (builder/config.js)     - a directory structure for collecting OS specific assets for building (builder/win/ for window s etc)",
  "main": "main.bundle.js",
  "scripts": {
    "dev": "electron main.js",
    "start": "electron main.bundle.js",
    "build": "npm run build:main && npm run build:ui",
    "build:main": "noderify main.js > main.bundle.js",
    "build:ui": "echo 'building ui...'",
    "pack": "electron-builder --config builder/config.js",
    "release": "npm run build && npm run pack -- --publish always"
  },
  "browserstlist": [
    "electron 18.0.1"
  ],
  "dependencies": {
    "ssb-ahoy": "^5.0.0",
    "ssb-backlinks": "^2.1.1",
    "ssb-db": "^20.4.1"
  },
  "devDependencies": {
    "electron-notarize": "^1.2.1",
    "noderify": "^5.1.0"
  }
}
